package gr.codehub.app;

public enum ActionMenu {
    ADD,
    REMOVE,
    DISPLAY,
    CLEAR,
    TOTALFILES,
    SAVE,
    LOAD,
    EXIT,
    SORT_BY_NAME,
    SORT_BY_TYPE,
    SEARCH_BY_NAME,
    TOTALSIZE,
    ERROR,
}
